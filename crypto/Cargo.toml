[package]
name = "spl-zk-token-crypto"
version = "0.1.0"
repository = "https://github.com/solana-labs/solana-program-library"
edition = "2018"

[features]
# "program" feature builds the library for the Solana program environment:
#
#      $ cargo build-bpf --features program --no-default-features
#
program = []

# "full" feature builds the library for the client, and includes functionality
# that is not compatible with the Solana program environment
full = [
  "rand",
]

default = [
  "full"
]

[dependencies]
arrayref = "0.3.6"
bincode = "1"
borsh = "0.9"
borsh-derive = "0.9.0"
byteorder = { version = "1", default-features = false }
clear_on_drop = { version = "0.2", default-features = false }
digest = { version = "0.8", default-features = false }
itertools = "0.8"
serde = { version = "1.0", features = ["derive"] }
sha3 = { version = "0.8", default-features = false }
subtle = { version = "2", default-features = false }
thiserror = { version = "1", optional = true }
zeroize = { version = "1.2.0", default-features = false, features = ["zeroize_derive"] }
merlin = { version = "2", default-features = false }
curve25519-dalek = { version = "2", default-features = false, features = ["u64_backend", "alloc", "serde"] }
rand = { version = "0.7.0", optional = true }

[dev-dependencies]
time = "0.1.40"
